{
  "name": "signalconso-website",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "analytics",
    "website",
    "mobile-tools"
  ],
  "engines": {
    "node": "20.9.0"
  },
  "devDependencies": {
    "node-addon-api": "^8.5.0",
    "ts-node": "10.9.2"
  },
  "scripts": {
    "installplaywright": "npx browserslist@latest --update-db --yes && npx playwright install --with-deps"
  },
  "packageManager": "yarn@3.2.1"
}
