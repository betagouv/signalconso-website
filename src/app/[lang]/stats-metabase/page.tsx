import {buildGenerateMetadata} from '@/core/metadatas'
import {StatsMetabase} from '@/reusablePages/statsMetabase'

export const generateMetadata = buildGenerateMetadata('statsMetabase')

export default StatsMetabase
