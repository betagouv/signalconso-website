import {CapacitorConfig} from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.signalconso.app',
  appName: 'signalconso-website',
  webDir: 'out',
  bundledWebRuntime: false,
}

export default config
